import { StyleSheet } from "react-native";

export const styles = StyleSheet.create({
  container: {
    flexWrap: "wrap",
    flexDirection: "row",
    gap: 12,
  },
  selectedItemsContainer: {
    paddingBottom: 200,
  },
});
